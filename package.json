{
  "name": "idle-dynasty-builder",
  "version": "0.0.3.7",
  "description": "Idle: Dynasty Builder - Lead your people from humble beginnings to a thriving dynasty in this idle strategy game.",
  "scripts": {
    "start": "npx serve . -l 8000",
    "dev": "npx serve . -l 8000 --no-clipboard",
    "build": "node scripts/update-version-json.js",
    "test": "vitest",
    "test:run": "vitest --run",
    "predeploy": "npm run build",
    "deploy": "npm run build && npx serve . -l 8000 --single"
  },
  "keywords": [
    "idle",
    "strategy",
    "browser-game",
    "village",
    "ai-battle",
    "dynasty",
    "kingdom"
  ],
  "author": "tanner-malt",
  "license": "MIT",
  "devDependencies": {
    "serve": "^14.2.0",
    "marked": "^4.3.0",
    "nodemon": "^3.0.0",
    "vitest": "^2.0.5",
    "jsdom": "^24.1.0"
  }
}